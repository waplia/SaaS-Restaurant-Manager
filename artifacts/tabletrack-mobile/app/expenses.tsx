// Top-level Expenses route. Reuses the owner expenses screen so
// cashiers can log petty-cash spend from their More menu without
// being blocked by the (owner)/_layout AuthGate.
export { default } from "./(owner)/expenses";
