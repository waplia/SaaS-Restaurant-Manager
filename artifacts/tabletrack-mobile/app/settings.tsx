// Top-level App Settings route. Reuses the owner settings screen so
// every role can adjust language / notification preferences from the
// shared More menu without duplicating the screen.
export { default } from "./(owner)/settings";
