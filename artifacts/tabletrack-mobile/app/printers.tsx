// Top-level Printers & terminals route. Reuses the owner printers
// screen so cashiers can manage receipt printers / KOT printers /
// terminals from their More menu without the (owner) AuthGate.
export { default } from "./(owner)/printers";
