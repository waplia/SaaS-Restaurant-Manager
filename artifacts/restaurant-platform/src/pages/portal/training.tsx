import MyTrainingPage from "@/pages/my-training";
// Reuses the existing /my-training page so the portal training section
// shows the same SOP + courses + certificates UI without duplicating logic.
export default MyTrainingPage;
