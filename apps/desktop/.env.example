# URL the desktop window loads on launch.
# Defaults to https://khanalagao.com/app/ in production.
APP_URL=https://khanalagao.com/app/

# Optional override for the API base URL (the web app normally figures this
# out from APP_URL). Leave empty unless you are pointing at a staging API.
API_URL=
